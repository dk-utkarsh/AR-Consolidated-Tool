# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
build/
.vercel/
.next/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# python (legacy — to be deleted in Phase 7)
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# legacy reference projects (Phase 7 cleanup — kept on disk only, not pushed)
/194QTDSVendorWiseReportBifurcationAndEmailSend/
/ChequeManagementSystem - Copy/
/UncategorizedSuspensePayments-main/
/dentalkart-gst-suite-master/

# real compliance input data (large + sensitive — never push)
/Non Complience/

# stray full-project duplicate (downloaded copy — never commit)
/AccountsConsolidated-main/

# Compliance Python engine: commit CODE only. Never the large test data
# (contains a 128MB xlsx > GitHub's 100MB limit) or generated outputs.
/dentalkart-gst-suite-master-UpdatedCodeNiranjanRaised/DataForComplienceReport/
/dentalkart-gst-suite-master-UpdatedCodeNiranjanRaised/*.xlsx
/dentalkart-gst-suite-master-UpdatedCodeNiranjanRaised/dentalkart-gst-suite-master/_run_outputs/

# dev scratch / repro scripts
worker/_repro.mts
worker/_verify_*.mts
worker/_verify_out/

# local dev scratch (real Zoho data cached during dev — never push)
worker/.devdata/
worker/.test-cheques/

# uploads / scratch
uploads/
output/

# misc
*.swp
*.bak

# runtime worker data (local, not committed)
data/
# env backups
.env.bak*
